// Package platform provides native game-window capture, PCM playback, and
// system speech. Go selects implementations by their OS suffixes and build
// constraints; window selection and playback policy are shared across targets.
package platform
